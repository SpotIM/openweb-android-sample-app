<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- light -->
    <font
        app:font="@font/tourney_condensed_light"
        app:fontStyle="normal"
        app:fontWeight="300" />

    <!-- regular -->
    <font
        app:font="@font/tourney_condensed_black"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <!-- italic -->
    <font
        app:font="@font/tourney_condensed_italic"
        app:fontStyle="italic"
        app:fontWeight="400" />

    <!-- medium -->
    <font
        app:font="@font/tourney_condensed_medium"
        app:fontStyle="normal"
        app:fontWeight="500" />

    <!-- semi-bold -->
    <font
        app:font="@font/tourney_condensed_semi_bold"
        app:fontStyle="normal"
        app:fontWeight="600" />

    <!-- bold -->
    <font
        app:font="@font/tourney_condensed_bold"
        app:fontStyle="normal"
        app:fontWeight="700" />
</font-family>
