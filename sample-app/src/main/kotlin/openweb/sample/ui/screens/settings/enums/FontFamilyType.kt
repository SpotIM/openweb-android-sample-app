package openweb.sample.ui.screens.settings.enums

/**
 * Font family configuration for SDK UI elements.
 *
 * - [Default]: Use SDK's default font family
 * - [Custom]: Apply custom font family from app resources
 */
enum class FontFamilyType {
    Default,
    Custom
}
